apiVersion: v1
kind: Service
metadata:
  name: {{ include "guessbook-app.fullname" . }}
  labels:
    {{- include "guessbook-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "guessbook-app.selectorLabels" . | nindent 4 }}
